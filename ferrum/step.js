// import { processArrow, streams, state_stack } from './process_arrow.js';
// import { expressions, functions } from './parse.js';


$("#stop").click(function() {
	// stop_running = true;
});

$("#run").click(function() {
	// running = true;
	// run();
});

$("#next").click(function() {
	// for (let i = 0; i < 100000; i++) {
	// 	if (step()) break;
	// }
});
